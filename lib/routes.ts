export const protectedRoutes = ["/admin", "/cloud", "/pm", "/devops"];

export const roleRoutes: Record<string, string[]> = {
  admin: ["/admin"],
  cloud: ["/cloud"],
  pm: ["/pm"],
  devops: ["/devops"],
};